/**
 * Placeholder component for future social media embeds.
 * Drop this into any page (e.g., Home) when ready to add
 * Instagram, Twitter/X, or other social feeds.
 *
 * Options when implementing:
 * - Official embed codes (iframe) from each platform
 * - react-embed or similar for unified handling
 * - CMS/social API integration for "Latest Posts"
 */
export default function SocialFeed() {
  return (
    <section className="mx-auto max-w-4xl px-4 py-16 sm:px-6 sm:py-24">
      <h2 className="text-center text-2xl font-semibold text-stone-800 sm:text-3xl">
        Latest Updates
      </h2>
      <p className="mt-4 text-center text-stone-500">
        Social feed and updates coming soon.
      </p>
    </section>
  );
}
